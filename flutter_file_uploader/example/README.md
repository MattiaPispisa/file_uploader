# Example

A Flutter example app showcasing various `flutter_file_uploader` features:
default upload, restorable chunked upload, custom self-managed card, file transformers pipeline, and a full-featured demo with drag-and-drop.
